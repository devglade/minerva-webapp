<%= javascript_tag "MakePostChannel(#{@spin.id})" %>
<h1>
  <span class="badge badge-primary"><%= @retrospect.title %></span>
  <span class="badge badge-primary"><%= @spin.title %></span>
</h1>
<br>
<%= link_to '글쓰기', new_retrospect_spin_post_path, remote: true, class: 'btn btn-block btn btn-outline-secondary' %>
<br>
<div id="board">
  <%= render partial: 'post', collection: @posts %>
</div>
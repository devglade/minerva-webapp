<%= javascript_tag "MakeSpaceChannel('#{current_user.email}')" %>

<div class="container container-space">
  <header class="header">
    <h1 class="headline">회고를 위한 공간</h1>
    <h4 class="sub-headline">회고를 진행할 공간을 생성하세요. 초대 받은 공간이 있다면 이곳에 표시됩니다.</h4>
    <h4 class="sub-headline"><%= @space_limit %>개의 공간까지 무료로 생성할 수 있습니다.</h4>
  </header>

  <ul class="list-spaces" id="space-board">
    <% @joined_spaces.each do |member| %>
      <% if member.space != nil %>
        <%= render partial: 'space', locals: {space: member.space} %>
      <% end %>
    <% end %>

    <li class="list-space-item" >
        <%= link_to new_space_path, class: 'content create-space', remote: true, id: 'create-button', style: @spaces_mine.count >= @space_limit ? 'display:none' : '' do %>
          <h1 class="">새로운 공간</h1>
        <% end %>
    </li>

  </ul>
</div>

<%= javascript_tag "MakePostChannel(#{@spin.id})" %>
<h1>
  <span class="badge badge-primary"><%= @project.title %></span>
  <span class="badge badge-primary"><%= @spin.title %></span>
  <span>그동안 있었던 일들을 모두 작성해보세요.</span>
</h1>
<br>
<%#= link_to '섹션만들기', new_space_project_spin_section_path(@space, @project, @spin) %>
<%#= link_to '글쓰기', new_space_project_spin_post_path(@space, @project, @spin), remote: true, class: 'btn btn-block btn btn-outline-secondary' %>
<br>
<div id="board" class="container-fluid" data-spin=<%= @spin.id %>>
  <%= render partial: 'sections/section', locals: {spin: @spin, sections: @spin.sections} %>
</div>
<%= javascript_tag "MakeSpinChannel(#{@project.id})"  %>
<div class="container" data-retrospect=<%= @project.id%>>
  <br>
  <h1>
    <span class="badge badge-primary"><%= @project.title %></span>
  </h1>
  <br>
  <%= link_to '회고 주제 등록하기', new_space_project_spin_path, remote: true, class: 'btn btn-block btn-secondary' %>
  <br>

  <div class="d-flex flex-wrap justify-content-center" id="spin-board">
    <%= render partial: 'spin', collection: @spins %>
  </div>
</div>